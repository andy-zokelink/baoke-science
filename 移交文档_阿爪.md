# Harry科学备考项目 — 移交文档

## 👤 目标用户
- **Harry**，深圳某小学
- 当前不及格 → 目标 80 分
- 终端：平板（移动端优先）

## 📊 数据概况
- 数据库：`D:/MKS/宝科小升初/baoke_learning.db`（SQLite，21张表）
- 688 道题（433对 / 183错 / 72未答）
- 21 个概念组，4级分类（★★★核心6个 / ★★常考84个 / ★易错5个 / ·边缘4个）
- 最大弱项：简单机械(13)、物质变化(12)、地球天文(10)

## 📁 核心文件

| 文件 | 用途 |
|------|------|
| `pages/科学备考.html` | 最终交付物（312KB，5栏目交互页面） |
| `baoke_learning.db` | SQLite 数据库（21表，所有题目+概念+追踪数据） |
| `db_utils_v2.py` | 数据库读写工具包 |
| `CLAUDE.md` | Claude Code 项目上下文 |
| `~/.claude/skills/mks-builder.md` | MKS 生成规范（Hermes 侧） |
| `~/.claude/settings.json` | Claude Code 配置（effortLevel: xhigh, kkidc API） |

## 🔧 环境配置

### Claude Code 配置 (`~/.claude/settings.json`)
```json
{
  "effortLevel": "xhigh",
  "model": "sonnet",
  "env": {
    "ANTHROPIC_BASE_URL": "https://ai-api.kkidc.com",
    "ANTHROPIC_AUTH_TOKEN": "sk-gtF..."
  }
}
```

### 数据库读取方式
```python
import sys; sys.path.insert(0, 'D:/MKS/宝科小升初')
from db_utils_v2 import *
wrong = get_wrong_answers()     # 全部错题（含溯源）
all_q = get_all_questions_with_answers()  # 全量题目
stats = get_concept_type_stats()  # 概念分类统计
```

## 🏗 构建流程

```
PDF(图片版) → Gemini Vision 逐页识别 → raw_questions 入库
→ DeepSeek 知识点归纳(纯文本) → concepts 概念组入库
→ Claude Code 读库生成 HTML → Hermes 复核 → 浏览器测试
```

## 📦 当前版本功能

### 5栏目
1. **知识概览** — 99概念按4级分类，5大领域折叠，学习路径5步
2. **知识卡片** — 99张3D翻转卡，正面概念名+标签，背面定义+陷阱
3. **模拟考试** — 131题池，随机抽30道全选择题，15分钟倒计时，选项随机分布
4. **错题本** — 80道错题+TOP12高频错误+出题规律+溯源
5. **思维导图** — 内联SVG，4层径向布局，4色区分+图例

### 已修复问题
- ✅ JS语法错误（转义符丢失）已修复
- ✅ 导航栏可点击切换
- ✅ 概念弹窗可打开
- ✅ 选项随机分布
- ✅ 只出选择题

## ⚠️ 待完成

1. **出卷功能** — 用户自定义题型/数量，生成标准试卷，导出PDF/Word
2. **正确率** — header显示63%但数据不对，需从DB重新计算
3. **死代码清理** — renderExamQuestions 中判断题/填空题路径（不会被触发但代码还在）
4. **Cloudflare Pages 部署** — wrangler 已配置但未创建项目
5. **概念详解** — 很多概念 definition 为空，需补充

## 🚀 如何继续

```bash
# 1. 打开 Claude Code 生成/修复
cd D:/MKS/宝科小升初
claude -p "你的任务描述" --effort xhigh --max-turns 80 \
  --dangerously-skip-permissions --allowedTools "Read,Edit,Write,Bash"

# 2. 浏览器测试
start pages/科学备考.html

# 3. 部署到 Cloudflare Pages（需先配置）
wrangler pages deploy pages/ --project-name=baoke-science
```

## 📝 Git 状态
- 仓库：`D:/MKS/宝科小升初/`（1个commit）
- 大部分文件为 untracked，建议先 `git add -A && git commit -m "科学备考HTML v1"`

---

**Hermes skill**: `mks-builder`（位于 `~/.claude/skills/mks-builder.md`）
**移交日期**: 2026年6月8日
